services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    environment:
      DB_PATH: /app/data/bot_data.db
      DOWNLOAD_DIR: /tmp/downloads
